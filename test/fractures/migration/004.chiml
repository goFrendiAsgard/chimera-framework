ins: action, config
out: output
verbose: 0
do:

  - |config.mongoUrl --> dbConfig.mongoUrl
  - |'test' --> dbConfig.collectionName

  - ins:
      mongoUrl: config.mongoUrl
      collectionName: 'test'
    out: dbConfig

  - if: action == 'up'
    do:
      - ins:
          - dbConfig
          - 'insert'
          - {'name': 'Ea'}
        do: |[$.mongoExecute]
        out: output

    else:
      - ins:
          - dbConfig
          - 'remove'
          - {'name':'Ea'}
        do: |[$.mongoExecute]
        out: output