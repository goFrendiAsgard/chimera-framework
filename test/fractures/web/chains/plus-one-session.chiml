ins: webState
out: response
do:
  - if: |'session' not in request || 'data' not in request.session
    do: 0 --> request.session.data
  - (parseInt(webState.request.session.data)+1) --> response.session.data
  - response.session.data --> response.data
