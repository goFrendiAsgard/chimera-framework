ins: webState
out: webState
series:

  - |(webState.config.localAfterRequestHook) --> hook

  - if: hook 
    do: |(_init_cwd + hook, webState) -> [$.loadJs('chimera-framework', 'run')] -> webState

  - |(webState.data) -> [$.loadJs( _chain_cwd + './js/afterRequestProcessor.js')] -> webState.data
