ins: action, config
out: output
verbose: 0
do:

  - |({mongoUrl: config.mongoUrl, collectionName:'test'}) --> dbConfig

  - if: action == 'up'
    do:
      # upgrade
      - |([config, dbConfig]) -> {$.print}

    else:
      # downgrade
      - |([config, dbConfig]) -> {$.print}
