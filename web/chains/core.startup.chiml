ins: st
out: st
vars:

  # routes
  routes:
    - route: '/'
      method: 'all'
      chain: 'index.chiml'

do:

  # preprocess routes
  - |(routes, _chain_cwd) -> {st.config.helper.getPreprocessedRoutes} -> st.config.routes

  # do upgrade migration 
  - |(st.config.migrationConfig) -> [$.loadJs('chimera-framework', 'migration.upgrade')]
