ins: state
out: state
do:
  - |basePath <-- state.config.basePath
  - |routes <-- state.config.routes

  # get cck routes and combine it with config's routes
  - |cckRoutes <- {$.loadJs(basePath + 'cck.js', 'getRouteList')}
  - |(routes, cckRoutes) -> {$.merge} -> state.config.routes
